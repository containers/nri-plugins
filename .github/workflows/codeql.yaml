name: Manually Run CodeQL Analysis

on:
  workflow_dispatch:

jobs:
  codeql:
    permissions:
      contents: read
      security-events: write
    uses: "./.github/workflows/common-codeql.yaml"
    with:
      export-report: true
